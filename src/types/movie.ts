interface Movie {
    title: string;
    release_date: string;
    poster_path: string
}

export default Movie

// Error: Objects are not valid as a React child (found: object with keys {adult, backdrop_path, genre_ids, id, original_language, original_title, overview, popularity, poster_path, release_date, title, video, vote_average, vote_count}). If you meant to render a collection of children, use an array instead.